syntax = "proto3";

package ark_msgs;

// A generic image message.
message Image {
  uint32 height = 1;
  uint32 width = 2;
  string encoding = 3;
  uint32 step = 4; // Full row length in bytes
  bytes data = 5;
}
