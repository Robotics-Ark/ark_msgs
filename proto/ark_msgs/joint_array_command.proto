syntax = "proto3";

package ark_msgs;

// Command for a set of joints.
message JointArrayCommand {
  enum Mode {
    POSITION = 0;
    VELOCITY = 1;
    TORQUE = 2;
  }
  repeated string name = 1;
  repeated float value = 2;
  Mode mode = 3;
}
