syntax = "proto3";

package ark_msgs;

// Ark message that is sent on each channel.
message ArkMessage {
  string src_node_name = 1;
  uint64 timestamp = 2;
  string payload_msg_type = 3;
  bytes payload = 4;
}
